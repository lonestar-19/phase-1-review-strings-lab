const currentUser ='Grace Hooper'

const welcomeMessage ='Welcome to Flatbook, ${Grace Hooper}!';

const excitedWelcomeMessage = welcomeMessage.toUpperCase ();

const shortGreeting = 'Welcome, ${currentUser.slice(0, 1)G}!';